<h2><%= product.name %></h2>
<a href="/product/<%= product.id %>/edit">Edit</a>
<%- include('../partials/deleteForm', { url: `/product/${product.id}` }) %> <%
if (productBycharacter.length > 0) { %>

<h2>Related Characters</h2>

<% productBycharacter.forEach(character => { %>
<img height="150" width="100" src="<%= product.coverImagePath %>" />
<% }) %> <% } %>
